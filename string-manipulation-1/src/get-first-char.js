/* exported getFirstChar */
// -storage for the first (letter)
// -locate the first letter
// -put the first letter into storage

function getFirstChar(string) {
  var firstChar = '';
  firstChar = string[0];
  return firstChar;
}
